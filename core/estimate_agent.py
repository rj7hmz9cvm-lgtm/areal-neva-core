def format_estimate_system_prompt(text: str) -> str:
    return (
        "Режим СМЕТА "
        "Если пользователь просит смету или расчет, давай структурированный ответ "
        "Материалы / Работы / Итого "
        "Показывай допущения кратко"
    )

def format_norms_system_prompt(text: str) -> str:
    return (
        "Режим НОРМЫ "
        "Отвечай строго по строительным нормам, ГОСТ, СНиП, СП РФ "
        "Если норма неочевидна, укажи что нужен точный документ"
    )
